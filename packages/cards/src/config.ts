const MAIN_PATH = 'https://microfrontend.fancy-app.site/api';

const config = {
  routes: {
    cards: `${MAIN_PATH}/cards/list`,
    cardsDetails: `${MAIN_PATH}/cards/details`,
  },
};

export default config;
