name: Build Transactions Project

on:
  push:
    paths:
      - 'packages/transactions/**'
  pull_request:
    paths:
      - 'packages/transactions/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'

      - name: Install Dependencies
        run: yarn install

      - name: Build Cards
        run: yarn workspace transactions build

      - name: Copy files to a temporary directory
        run: |
          mkdir transactions
          cp -R packages/transactions/dist/* cards/

      - name: Deploy to Server
        uses: appleboy/scp-action@master
        with:
          host: 138.68.115.124
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "transactions/*"
          target: "/usr/share/nginx/html/microfrontend/apps"
